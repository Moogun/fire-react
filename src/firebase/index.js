import * as firebase from './firebase'
import * as auth from './auth';
import * as db from './db';

import * as teacherProfile from './teacherProfile'
import * as newCourse from './newCourse'

export {
  firebase,
  auth,
  db, 
}
